#include "main.h"
/**
 * main - Entry point
 * Description: 'the program's description'
 * Return: Always 0 (Success)
 */

int _islower(int c)
{
	if ((c > 97) && (c < 123))
	{
		return (1);
	}
	else
	{
		return(0);
	}
}
